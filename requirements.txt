# Core bot dependencies
python-telegram-bot==21.6
anthropic>=0.40.0
apscheduler==3.10.4
python-dotenv==1.0.0

# Admin dashboard
PyQt5>=5.15.10

# Search
tavily-python>=0.3.0

# Windows service (optional — only needed for service.py install)
pywin32>=306; sys_platform == "win32"
